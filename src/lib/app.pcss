@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
    :root {
        --border-btn: 2px;
    }
    * {
        @apply select-none drag-none;
    }
}

@layer utilities {
    .container {
        @apply max-w-screen-md;
    }
}
